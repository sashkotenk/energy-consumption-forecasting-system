services:
  db:
    ports:
      - "127.0.0.1:${DB_PORT:-5432}:5432"
    networks:
      - backend
      - development

  nginx:
    ports:
      - "127.0.0.1:${APP_HTTP_PORT:-8080}:8080"

networks:
  development:
